# DeveloperHCR:AI Agent — BETA v1.0 Shell Final

Additive shell repair; existing application catalog is preserved.

- Start/App menu and touch/click behavior repaired.
- Taskbar search now mirrors into the Start menu and Enter opens the first match.
- Desktop shortcuts remain visible on Windows and Android layouts.
- Landscape is responsive instead of forcibly rotating the phone viewport.
- Quick-launch rail: Files, Terminal, AI Chat, HCR AI Agent, Settings, Control Panel.
- Date/time and BETA status stay visible.
- BAT/CMD files use a dedicated Batch Runner; Windows Admin/Owner can explicitly launch them in a new local console.
- start_windows.bat keeps the launcher terminal visible while the local browser UI opens.

A browser cannot directly execute a BAT file; the local Windows command processor is used when running on Windows.
