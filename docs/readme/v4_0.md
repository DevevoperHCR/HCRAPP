# DeveloperHCR v4.0

This release fixes the v3.9 startup `async is not defined` runtime fault, starts the authentication gate only after the complete frontend has loaded, and restores the Windows-like HCR Start/app launcher.

Login UI is intentionally minimal: logo, username, password, Start, Login Troubleshoot and README.txt.
