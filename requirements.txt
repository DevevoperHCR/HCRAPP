fastapi>=0.110.0
uvicorn[standard]>=0.29.0
psutil>=5.9.8
websockets>=12.0
httpx>=0.27.0

# Optional (only needed for GGUF chat - the app works fine without it,
# Model Manager will just report GGUF as "runtime missing"):
# llama-cpp-python

# v0.5 Jarvis optional offline voice/screen controls (install only if needed)
# vosk
# sounddevice
# numpy
# pyttsx3
# mss
# pillow
# screen-brightness-control
